package lecture04.section04.ObjectOrientedProgramming;

// We do not allow to have property or implementation methods in interface. we just allow abstract methods.
public interface AnimalInterface {
    void makeSound();


    String name();
}
